#!/usr/bin/env node

console.error(
  '[build-html] Deprecated: this repository is Next.js App Router only. ' +
    'Use `npm run build` and `npm run start`. Legacy static HTML build is disabled.',
)
process.exit(1)
